# Legacy Plans Folder

This folder is retained temporarily during phased migration.

Canonical locations are now:
- Deck outputs: `quality_reports/decks/`
- Deck scripts: `quality_reports/decks/src/`
- Deck data: `quality_reports/decks/data/`

Use `quality_reports/INDEX.md` for the migration path map.
